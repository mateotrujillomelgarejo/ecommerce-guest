server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mateotrujillomelgarejo/ecommerce-config-repo
          search-paths: '.'
          default-label: main

management:
  endpoints:
    web:
      exposure:
        include: health,info